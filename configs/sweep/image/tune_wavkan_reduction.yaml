# @package _global_
#
# Stage 2 of the staged wavkan sweep (siehe README "Gestaffeltes Sweeping"):
# model.wavelet_type + Reduction-Methode/-Knobs. Architektur
# (model.n_hidden_layers/hidden_width_*) und Optimizer (lr/weight_decay)
# werden NICHT hier gesweept, sondern muessen als fixe Overrides von Stage 1
# (tune_wavkan_arch.yaml) mitgegeben werden, z.B.:
#   sbatch --export=ALL,EXPERIMENT=...,SWEEP=image/tune_wavkan_reduction,\
#     EXTRA_weak_lensing="model.n_hidden_layers=2 model.hidden_width_0=494 \
#     model.hidden_width_1=409 optimizer.lr=8e-4 optimizer.weight_decay=1e-5" \
#     scripts/tune_wavkan.submit
defaults:
  - /sweep/image/_reduction_sweep
  - override /hydra/sweeper: optuna
  - override /hydra/sweeper/sampler: tpe
  - override /model: wavkan

seed: 42

training:
  epochs: 50   # max budget; early stopping (configs/training/default.yaml) ends trials sooner

hydra:
  sweeper:
    sampler:
      seed: ${seed}
      n_startup_trials: 10
    direction: minimize

    n_trials: 50
    n_jobs: 1
    params:
      model.wavelet_type: choice(mexican_hat, morlet, dog, meyer, shannon)
