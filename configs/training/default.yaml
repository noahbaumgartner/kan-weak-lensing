epochs: 500
batch_size: -1
lamb: 0.001
device: auto

# Validation-loss early stopping (see configs/training/adam.yaml for details).
# es_min_delta is a *relative* threshold (fraction of the current best).
early_stopping: true
es_patience: 12
es_min_delta: 0.001
es_restore_best: true
